# OpenAI API для генерации резюме (обязательно)
OPENAI_API_KEY=sk-...

# GitHub для сохранения резюме (опционально)
GITHUB_TOKEN=github_pat_...
GITHUB_OWNER=your-username
GITHUB_REPO=your-repo
GITHUB_BRANCH=main
GITHUB_FILE_PATH=articles

# PostgreSQL (используются docker-compose дефолты)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=habr_scraper
POSTGRES_USER=habr_user
POSTGRES_PASSWORD=habr_password

# Лимит статей для обработки за один запуск (по умолчанию без лимита)
ARTICLES_LIMIT=10
